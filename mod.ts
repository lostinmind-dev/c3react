import { app } from './lib/core.ts';

export {
    pointer,
    mouse,
    keyboard,

    Component, type ExtractObjectInstType,
    EventsHandler,
    Layout,
    Modal,

    useComponent,
    useParent,
    useChild,
    useChildren,
    useObject,
    useObjects,
    useEffect,

    useTouched,
    useMouseOver,

    utils,
} from './lib/core.ts';

export default app;
